package GrammarExtend;

import java.util.HashMap;

//位运算：基于二进制进行运算
//&(按位与)效果和取余一样但是效率高   >>(右移)(移动一位效果等于整除二) <<(左移)(等同于乘二)
//^(异或)  hashCode()方法中的hash()方法使用了^
public class Test {
    public static void main(String[] args) {
    }
}
